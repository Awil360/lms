pipeline {
    agent any

    stages {
        stage('sonar analysis') {
            steps {
                echo "code quality analysis"
                sh 'cd webapp && sudo docker run  --rm -e SONAR_HOST_URL="http://35.86.175.8:9000" -e SONAR_TOKEN="sqp_c721a8c2120e621087cab4142fb2ae90bf2c471e" -v  ".:/usr/src" sonarsource/sonar-scanner-cli -Dsonar.projectKey=lms '
                echo "code completed"
            }
        }
        stage('Test') {
            steps {
                echo "TestinggG.."
            }
        }
        stage('Deploy') {
            steps {
                echo "Deployingg...."
            }
        }
    }
}